<!doctype html>
<html>
	<html>
		<meta charset="UTF-8"/>
		<script src="https://cdn.socket.io/4.0.1/socket.io.min.js"></script>
		<script src="/JS/webrtc/signalingserver.js"></script>
		<title>Virtual Folder</title>
	</html>
	<body>
		<h1>Folder Page</h1>
		<script>
			const params = <%- JSON.stringify(params) %>;
			const folder_name = params.folder_name;
			const folder_pass = params.folder_pass;
			const socket = io("http://localhost:7777");
			console.log(`aaaa ${folder_name} ${folder_pass}`);
			init_signaling_client(socket,folder_name,folder_pass);
		</script>
	</body>
</html>
